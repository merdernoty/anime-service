package routes

import (
    "github.com/gin-gonic/gin"
)

func SetupRoutes(
    r *gin.Engine,
    // userController *controllers.UserController,
    // authMiddleware middleware.AuthMiddleware,
) {
    //v1 := r.Group("/api/v1")
    
    // RegisterUserRoutes(v1, userController, authMiddleware)

}